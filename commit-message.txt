fix: gameplay reconnect, admin role system, and gameplay auto-advance

- Fix gameHandler: prevent skipNextQuestion double increment after DB reload,
  emit question on player reconnect, fix missing startQuestionTimer function
- Add skipNextQuestion flag in GameState type to track reload-from-DB flow
- Admin: full role system with AdminRoute guard, login redirect by role,
  admin controller (17 endpoints), enhanced dashboard stats (11 cards),
  user management (lock/unlock/role/delete/restore), quiz management
  (delete/restore/hide/show deleted), session end, homework view
- JwtAuthGuard: check isActive on user from DB, block inactive accounts
- AdminService: enhanced stats with active/locked users, published/draft
  quizzes, finished sessions, total players, 30-day activity metrics
- AdminPage: move TABS inside component (fix t() at module level),
  real data from API, role filter, visibility filter, deleted filter,
  empty states, pagination, confirm dialogs
- AdminRoute component: protect /admin route by checking user.role === 'admin'
- LoginPage: redirect admin to /admin, others to /dashboard after login
- App.jsx: wrap /admin route under AdminRoute
- New API functions: restoreUser, restoreQuiz, endSession
- Locales: add 25 new admin translation keys (en + vi)
